events {
	worker_connections 1024;
}

http {
	server 
	{
		listen 80;
		server_name cponmamju.42;

<<<<<<< HEAD
	server {
		listen 443 ssl;

		server_name _;

		ssl_certificate /etc/ssl/certs/cponmamju.fr_cert.pem;
		ssl_certificate_key /etc/ssl/private/cponmamju.fr_key.pem;

		ssl_protocols		TLSv1.2 TLSv1.3;

		location / {
			proxy_pass http://localhost:8000/;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header X-Forwarded-Proto $scheme;
		}

		location /ws/ {
			proxy_pass http://localhost:8000;
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection "upgrade";
			proxy_read_timeout 86400;

=======
		location /ws/ {
			proxy_pass http://central:7777;  # Point to the central container's WebSocket server
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection 'upgrade';
			proxy_set_header Host $host;
>>>>>>> 534b6f482d8c418ce526fee416a441746fe077b8
		}
	}
}
